namespace Tools
{
    public enum PlayerState
    {
        Moving,
        Idle
    }

    public enum GameState
    {
        INITIALIZE = 0,
        INITIALIZEGAME = 1,
        MENU = 2,
        PLAY = 3,
        PAUSE = 4,
        FINISH = 5,
    }
}
